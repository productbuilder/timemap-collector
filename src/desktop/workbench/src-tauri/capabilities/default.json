{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for workbench window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
